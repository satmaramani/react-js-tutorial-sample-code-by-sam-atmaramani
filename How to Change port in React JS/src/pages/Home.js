import "../App.css";
const Home = ()=>{

    return <><div className="App-header"> Port change app = ? Inside the env file (.env) I have provided the changed port number <br />  
        i.e. PORT=4000 instead of default port 3000 - hence this react JS app will run on http://localhost:4000
    <br /></div></>
   
}

export default Home;